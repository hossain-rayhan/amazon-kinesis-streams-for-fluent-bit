
[SERVICE]
    Flush       5
    Log_Level   debug

[INPUT]
    Name    forward
    Listen  0.0.0.0
    Port    24224

[OUTPUT]
    Name            kinesis
    Match           *
    region          us-west-2
    # Replace with your kinesis data stream name
    stream          fluent-bit-kinesis-test
    partition_key   container_id
    data_keys       log
    append_newline  true